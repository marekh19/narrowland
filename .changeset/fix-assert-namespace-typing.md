---
"narrowland": patch
---

Fix `assert` namespace typing by adding explicit `AssertNamespace` interface (required by TypeScript for assertion functions) and add namespace integration tests for `is`, `assert`, and `ensure`
