---
"narrowland": minor
---

- Implement `ensure` family to extend `is` and `assert` branches of the library with directly returning the narrowed value
- Mark `ensure` as deprecated, replaced with `ensureDefined`. Will be removed in v2.0.0
